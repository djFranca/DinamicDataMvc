@model IEnumerable<DinamicDataMvc.Models.MetadataListModel>
@{
    ViewData["Title"] = "TestMetadataList";
    string branches = String.Empty;
}

<section>
    <div>
        @using (Html.BeginForm("", "", FormMethod.Get))
        {
            @*<span>
                @Html.LabelFor(model => model.Metadatas[0].Name)
                @Html.TextBoxFor(model => model.Metadatas[0].Name, new { @size = "10px" })
                @Html.LabelFor(model => model.Metadatas[0].Version)
                @Html.TextBoxFor(model => model.Metadatas[0].Version, new { @size = "10px" })
                @Html.LabelFor(model => model.Metadatas[0].Branches)
                @Html.ListBoxFor(model => model.Branches, Model.Branches, new { size = 1 })
            </span>*@
            <input type="submit" value="" style="background:url('/images/search_logo.png') no-repeat center; width:60px; height:40px" />
        }
    </div>
</section>
<br />
<section>
    <div class="text-body">
        <h4>Metadata</h4>
    </div>
    <div>
        <table class="table">
            <thead>
                <tr>
                    <th valign="middle" align="center">
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th valign="middle" align="center">
                        @Html.DisplayNameFor(model => model.Version)
                    </th>
                    <th valign="middle" align="center">
                        @Html.DisplayNameFor(model => model.Date)
                    </th>
                    <th valign="middle" align="center">
                        @Html.DisplayNameFor(model => model.Branch)
                    </th>
                    <th valign="middle" align="center">
                        @Html.DisplayNameFor(model => model.State)
                    </th>
                </tr>
            </thead>
            <tbody id="ProcessList">
                @foreach (var item in Model)
                {
                <tr>
                    <td>
                        @Html.ActionLink(@Html.DisplayTextFor(modelItem => item.Name), "ProcessDetails", FormMethod.Get)
                    </td>
                    <td>
                        @Html.DisplayTextFor(modelItem => item.Version)
                    </td>
                    <td>
                        @Html.DisplayTextFor(modelItem => item.Date)
                    </td>
                    <td>
                    @foreach (string branch in item.Branch)
                    {

                        <span>
                            @Html.DisplayTextFor(s => branch)
                        </span>
                            
                        
                    }
                    </td>
                    <td>
                        @Html.DisplayTextFor(modelItem => item.State)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /*ID = item.Id*/ }) |
                        @Html.ActionLink("Delete", "Delete", new { /*ID = item.Id*/ })
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </div>
</section>

